# Opus spot-check reads, 2026-08-27

⚠ THESE ARE NOT `raw/opus`. That directory is the blind full-tranche read and
stops at 106; `batch_cold` refuses to run where it exists, and adding pages
here must never make it think 107+ has been read. These two columns were read
one at a time, with John authorising each, to MEASURE what the sweeps missed —
not to spine anything.

Each was transcribed from the 400dpi scan without opening `work/reconciled`,
then diffed against it.

    114-L   3 differences, all corpus defects, all ruled and applied
              21  `Zeller iudicat I,`  → `II,`
              23  `mentio ft.`         → `mentio fit.`   (the fi ligature)
              52  `Wz I, 528`          → `Wz II, 528`
    115-L   0 differences

Two more differences on 114-L were MY error, not the corpus: I read an iota
subscript in `διήρηται` at lines 24 and 46 that is not there — show-through
from the line below. This file carries the corrected transcription, so a diff
against it now comes back empty for both columns.
