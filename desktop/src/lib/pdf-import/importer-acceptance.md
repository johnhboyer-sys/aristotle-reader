# Importer acceptance checklist

Check items off as they're proven, with the proving test named.

- [~] Every gutter tic on a geometry-intact page detected and bound, or explicitly flagged (collapse / review) — PROVEN for the Lennox + synthetic Reeve gold fixtures (gutter-gold.test.ts, gutter-reeve.test.ts: all 20 tics across the two fixtures bind exactly). NOT fully proven at corpus scale: the real 176-page NE slice (gutter-slice.integration.test.ts, env-gated) surfaces a genuine gap — a printed mark that's actually MISSING from the source is correctly flagged (dropped-line), not silently bound, which is the designed behavior, but it means "detected and bound" has a real, characterized exception class (see implementation-notes.md). Annotated rather than checked because Phase 1 scope explicitly includes "or explicitly flagged," and that's what happens — but the >2500-tic corpus-coverage assumption in the integration test does not hold for a single-work slice this size, so full-corpus confidence is not yet established.
- [x] Lennox fixture reproduces exactly (all 8 rows) — gutter-gold.test.ts, `detects exactly 8 tics, in order, with correct column/line/anchor`.
- [x] 5-line cadence self-audit closes; missing tic → flagged dropped line, never interpolated — gutter-reeve.test.ts `has zero dropped-line flags across all three pages` (clean-geometry case, nothing to flag) plus the real-slice integration test's `dropped-line:1119b20` (page 50) case, which demonstrates the flag firing on genuine real-world input: the missing 1119b20 mark is flagged, never fabricated with an interpolated line value — `promoteByCadenceMonotonic`'s `nextMultiplesOf5Between` in gutter.ts only ever pushes a flag string, it never synthesizes a Tic.
- [x] Headers stripped; divisions tagged {b.c} with correct inheritance; titles captured-unaligned or correctly absent — divisions.test.ts (Clarendon keyworded BOOK/CHAPTER incl. Roman + spelled-out with correctly-absent titles; glued endnote markers; corroboration/inheritance rows incl. book-heading-authority and restated-b adoption; long-title center-alignment capture; preamble; seam restart) plus the real-slice integration test's division invariants (gutter-slice.integration.test.ts: 11 books, 117 chapters all with captured titles, correct b inheritance on every chapter — zero corroboration mismatches — and the MM seam flagged, never renumbered). Header stripping itself is Phase-1-proven (position-based line-0 strip, gutter-gold/gutter-reeve incl. the header-trap case).
- [x] Footnotes separated; scope inferred and stated; verso/footnote never confused — footnotes.test.ts (spec §C1 real-shaped cases: glued marker↔note pairing, running-head-glued star work-level note, multi-line note assembly, AM1 diagram display-line preservation, MM multi-note block + body-section-number rejection + verbatim Bekker cross-ref + inert leading dagger, footnote-note-unmatched; §C2 all six synthetic rows: recto tic-vs-marker collision, tic-ambiguous withholding, per-chapter/empty-chapter/conflict scope scoring, duplicate-number pairing) plus the real-slice integration test's footnote invariants (gutter-slice.integration.test.ts: 224 numbered + 2 star notes measured against the real 176-page slice, zero unmatched notes, one correctly-flagged unmatched marker, NE scope verdict continuous with 95 discriminating observations, MM measured as genuinely underdetermined with only 2 notes). Format round-trip (translation-file-footnotes.test.ts): the §B2 worked example verified numerically, per-chapter/star-dagger label round-trips, and the legacy byte-identical guarantee verified directly against the actual pre-Phase-3 module. "Verso/footnote never confused" is structural: note parsing is confined to the bottom-furniture region (re-derived independently in footnotes.ts, not merely inherited from gutter.ts), proven by the MM body-section-number-rejection case where flush-left numbers identical in grammar to real notes sit just above the furniture boundary and are never scanned as notes.
- [ ] Paragraphs preserved; anchors survive reflow (text stream unchanged)
- [ ] Edition's numbering captured verbatim — no normalization, no renumbering
- [ ] No printed markers → clean refusal with the stated message
- [ ] Collapsed page → user offered re-extract or page-level-only
- [ ] Import output demonstrably local-only (no sync path can reach it)
