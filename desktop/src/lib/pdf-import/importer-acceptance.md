# Importer acceptance checklist

Check items off as they're proven, with the proving test named.

- [~] Every gutter tic on a geometry-intact page detected and bound, or explicitly flagged (collapse / review) — PROVEN for the Lennox + synthetic Reeve gold fixtures (gutter-gold.test.ts, gutter-reeve.test.ts: all 20 tics across the two fixtures bind exactly). NOT fully proven at corpus scale: the real 176-page NE slice (gutter-slice.integration.test.ts, env-gated) surfaces a genuine gap — a printed mark that's actually MISSING from the source is correctly flagged (dropped-line), not silently bound, which is the designed behavior, but it means "detected and bound" has a real, characterized exception class (see implementation-notes.md). Annotated rather than checked because Phase 1 scope explicitly includes "or explicitly flagged," and that's what happens — but the >2500-tic corpus-coverage assumption in the integration test does not hold for a single-work slice this size, so full-corpus confidence is not yet established. Phase 5 adds a SECOND real, independently-measured slice (cat-slice.integration.test.ts, env-gated, the 27-page Reeve Categories extraction): 236/236 tics detected with a completely EMPTY flag histogram — zero dropped lines, zero non-monotonic, zero side-ambiguity — corroborating evidence from a structurally different edition (single-book, 1-2-digit Bekker pages) but still one more slice, not corpus scale.
- [x] Lennox fixture reproduces exactly (all 8 rows) — gutter-gold.test.ts, `detects exactly 8 tics, in order, with correct column/line/anchor`.
- [x] 5-line cadence self-audit closes; missing tic → flagged dropped line, never interpolated — gutter-reeve.test.ts `has zero dropped-line flags across all three pages` (clean-geometry case, nothing to flag) plus the real-slice integration test's `dropped-line:1119b20` (page 50) case, which demonstrates the flag firing on genuine real-world input: the missing 1119b20 mark is flagged, never fabricated with an interpolated line value — `promoteByCadenceMonotonic`'s `nextMultiplesOf5Between` in gutter.ts only ever pushes a flag string, it never synthesizes a Tic. cat-slice.integration.test.ts corroborates with a second real slice (27 pages) with zero cadence gaps at all — the audit has nothing to catch there, which is itself a measured, honestly-reported fact, not an assumption.
- [x] Headers stripped; divisions tagged {b.c} with correct inheritance; titles captured-unaligned or correctly absent — divisions.test.ts (Clarendon keyworded BOOK/CHAPTER incl. Roman + spelled-out with correctly-absent titles; glued endnote markers; corroboration/inheritance rows incl. book-heading-authority and restated-b adoption; long-title center-alignment capture; preamble; seam restart) plus the real-slice integration test's division invariants (gutter-slice.integration.test.ts: 11 books, 117 chapters all with captured titles, correct b inheritance on every chapter — zero corroboration mismatches — and the MM seam flagged, never renumbered). Header stripping itself is Phase-1-proven (position-based line-0 strip, gutter-gold/gutter-reeve incl. the header-trap case). "Unaligned" stays true all the way to render (revised 2026-07-06, John's call): `imports.test.ts`'s `resolveImportTitle` describe block proves a captured title resolves by plain `book.chapter` lookup only — never touching `RossPiece.text`/offsets or the shared chapterTitles heading map — and Reader.svelte renders it as a sibling `.ross-chapter-title` div beside (never inside) the offset-bearing `flow` array. **Phase 5 §1 (bare-numeral chapters, single-book works)**: divisions.test.ts's "bare-numeral chapters (Phase 5 spec §1)" describe block (8 tests: clean sequence acceptance + book=1 keying + single-book-work flag firing exactly once; b.c-mode-wins silent rejection; false-positive stray-numeral rejection before the true "1"; the one-chapter forward-gap tolerance and its flag, vs. a wild jump/backward value both rejected outright; free-floating title-candidate non-capture after a rejected numeral) plus cat-slice.integration.test.ts's real-slice proof (env-gated, the 27-page Reeve Categories extraction): all 15 chapters detected as {1.1}..{1.15}, every one titled, zero Book divisions anywhere, `single-book-work` doc flag fires exactly once, zero division-level audit flags — the app's existing single-book spine convention (book=1 implicit, verified against `app/src/lib/works.ts`'s `isBookless()`/`workPath()` and the manifest format) is what the implicit book=1 keys into, never a new bare-`c` scheme.
- [x] Footnotes separated; scope inferred and stated; verso/footnote never confused — footnotes.test.ts (spec §C1 real-shaped cases: glued marker↔note pairing, running-head-glued star work-level note, multi-line note assembly, AM1 diagram display-line preservation, MM multi-note block + body-section-number rejection + verbatim Bekker cross-ref + inert leading dagger, footnote-note-unmatched; §C2 all six synthetic rows: recto tic-vs-marker collision, tic-ambiguous withholding, per-chapter/empty-chapter/conflict scope scoring, duplicate-number pairing) plus the real-slice integration test's footnote invariants (gutter-slice.integration.test.ts: 224 numbered + 2 star notes measured against the real 176-page slice, zero unmatched notes, one correctly-flagged unmatched marker, NE scope verdict continuous with 95 discriminating observations, MM measured as genuinely underdetermined with only 2 notes). Format round-trip (translation-file-footnotes.test.ts): the §B2 worked example verified numerically, per-chapter/star-dagger label round-trips, and the legacy byte-identical guarantee verified directly against the actual pre-Phase-3 module. "Verso/footnote never confused" is structural: note parsing is confined to the bottom-furniture region (re-derived independently in footnotes.ts, not merely inherited from gutter.ts), proven by the MM body-section-number-rejection case where flush-left numbers identical in grammar to real notes sit just above the furniture boundary and are never scanned as notes. **Phase 5 additions**: (a) clarendon.test.ts is the first end-to-end proof of a LOCKED 'per-chapter' verdict (the existing §C2 synthetic row only exercises the scoring math in isolation) — a synthetic 4-chapter fixture with footnotes reset to 1 every chapter reaches the scope machine's own SCOPE_DECIDE_N=3 discriminating-observation threshold and locks `footnote-scope:per-chapter`, with every label scoped `<book>.<chapter>.<N>` (`[^2.1.1]` … `[^2.4.1]`) and round-tripping through parseTranslationFile; (b) cat-slice.integration.test.ts measures a genuinely NEW, honest finding: a single-book work can NEVER discriminate 'continuous' from 'per-book' scope (book never crosses a boundary, so the two hypotheses predict identically for every observation) — verdict stays null (12 discriminating observations, both scopes still alive), and emission's documented fallback (`verdict ?? 'continuous'`) is what actually labels the output; this is a structural fact about single-book works, not a gap in the algorithm. **Bug found and fixed via this real-slice measurement** (2026-07-06, logged in implementation-notes.md): the shared display-shape "bridge past a blank gap" logic in gutter.ts's `findBottomFurnitureStart` / footnotes.ts's `computeNoteBlockStart` (added in Phase 3 for a footnote's own interior diagram) was ALSO silently absorbing the real ten-categories body table's first three rows into the footnote/furniture region — genuine body content, dropped from the emitted text entirely, whenever a display-shaped table sits directly above the footnote block with only one blank-line gap. Fixed by anchoring the block's start on the TOPMOST note-starter line actually reached rather than however far the bridge wandered; the entire pinned NE-slice suite (gutter-slice.integration.test.ts, convert-slice.integration.test.ts) was re-run and is BYTE-IDENTICAL before and after the fix.
- [~] Paragraphs preserved; anchors survive reflow (text stream unchanged) — EMISSION SIDE PROVEN: emit.test.ts (golden three-page conversion: paragraph joins across both page boundaries, +4/+3 indent starts, hyphen joins, single-\n\n-only body) and convert-slice.integration.test.ts (real 176-page slice: 974 paragraphs with zero empties, every mid-sentence-looking break hand-characterized as verse/display, and all 20 John-confirmed gold anchors — 1094a1 "Every" … 1095b1 "For" across a hyphen join — landing at exactly their words in the reflowed clean text; splitChapters yields all 117 chapters; alignImportedChapter returns confidence:'tagged' anchors at the tags' exact offsets). Annotated rather than checked because the claim's other half — that the RENDERED app shows those paragraphs/anchors, specifically for a PDF-converted import via this converter's own tagged output — has not itself been driven through the app in this session (Phase 4B's in-app verification note above covers footnote popups/title placement/import summary, not this converter's paragraph reflow specifically); the emitted text stream itself is proven unchanged through parse→split→align, which is what stays checked. **Phase 5 DISPLAY-BLOCK proof on a real page break** (cat-slice.integration.test.ts's "ch.4 display block" test): the ten-categories table (Categories ch. 4) genuinely SPANS a page break, with footnotes + a folio + a running head printed between its two halves, and carries a real gutter tic on one of its rows (`Where … 2a1`, flagged `display-block-anchor`). All ten rows are asserted to appear in the chapter's clean text in PRINTED ORDER with none of the intervening furniture leaking in — the first proof of Phase 4A's display-block detector (`emit.test.ts`'s own display-block test is synthetic) against a genuine multi-page real-world table, and the case that surfaced the furniture-boundary bug documented in the row above.
- [x] Edition's numbering captured verbatim — no normalization, no renumbering — emit.test.ts + convert-slice.integration.test.ts: every emitted tag is the printed number ({1181b25} at the MM seam is the printed 1181b25, not a corrected 1181a value; MM's restart Book 1/1.1 kept verbatim behind the seam warning), suppressed tics are counted and OMITTED (never re-valued), dropped lines flagged (1119b20) never interpolated, titles captured verbatim including collisions. Grammar side: translation-file-tags.test.ts pins legacy parses byte-identical under the extended TAG grammar. **Phase 5**: cat-slice.integration.test.ts measures the subtlety directly rather than guessing at it — Categories ch.4 opens on a printed bare "25" (no page/column digits at all), which emits the BARE line tag `{25}` verbatim (not a fabricated composite `{1b25}`); only the PARSED citation resolves the full "1b25" address via the running column context, settling the phase-5 spec's own tentative "{1b25}?" guess with a measured, hand-verified answer.
- [x] No printed markers → clean refusal with the stated message — emit.test.ts refusal case: prose-only pages → ConvertRefusal with exactly the Phase-1 §12 message and honest {pages, nonEmptyPages} scan stats; no partial output.
- [x] Collapsed page → user offered re-extract or page-level-only — CONVERTER SIDE (as before): emit.test.ts §3.6 case (synthetic collapsed page → ConvertNeedsChoice with the collapsed page list and NO partial output; re-invoked with pageLevelOnly:true → body emitted, full-form tags kept, bare tics suppressed and counted). UI SIDE (Phase 4B): ImportDialog.svelte's `convert-choice` step — "N page(s) lost their print layout in extraction (pages …)" with two buttons, "Cancel (re-extract)" (`onClose(null)`) and "Import with page-level anchors only" (`retryPageLevelOnly`, which re-invokes `acceptText` on the SAME held upload with `{pageLevelOnly: true}`, reusing the converter's own two-way contract rather than re-prompting for a file).
- [x] Import output demonstrably local-only (no sync path can reach it) — storage path: `desktop/src/lib/imports.ts`'s `tauriStore()` writes only to `appDataDir()/translations/<work>/<id>.{md,original,map.json}` (local filesystem via `@tauri-apps/plugin-fs`), the browser-harness fallback writes only to `localStorage`; neither backend makes a network call. New-in-Phase-4B code paths (the PDF converter, `resolveImportTitle`/`getImportTitle`, `resolveImportFootnote`/`getImportFootnote`, the three `__ARISTOTLE_IMPORT_*` window hooks) are all pure/synchronous in-process computation — grepped `fetch(`/`XMLHttpRequest`/`WebSocket`/`http(s)://` across imports.ts, the whole pdf-import/ module tree, and ImportDialog.svelte: zero matches. The pre-existing `fetchBook`/`fetchChapters` calls `runImport` already made (unchanged by this task) read the corpus's OWN bundled/local data root to align against — never the user's imported text — and are not part of the new surface this task adds. Phase 5 added no new I/O surface (bare-numeral chapter detection and the furniture-boundary fix are both pure, in-process, synchronous).

---

## Phase 5 audit (2026-07-06)

Went through every row above against the Phase 5 additions (bare-numeral chapter
grammar §1, the real Reeve Categories slice §2, the synthetic Clarendon per-chapter
fixture §3). Nothing regressed: the full test suite (unit + both env-gated
integration tests, ARISTOTLE_REEVE_SLICE and ARISTOTLE_REEVE_CAT_SLICE set together)
is green, and the two real-slice suites' pre-existing pins (NE: 176 pages; Categories:
newly added, 27 pages) are both exact.

State after this pass: 8 rows checked, 2 annotated (both pre-existing caveats,
unchanged in kind by Phase 5 — corpus-scale tic coverage, and in-app rendering
verification of the converter's own paragraph/anchor output specifically). No row
was newly annotated or newly checked by Phase 5 alone; Phase 5's job was to extend
the PROOF underneath rows that already existed (bare-numeral chapters under the
divisions row; the real Categories slice and the locked per-chapter verdict under
the footnotes/paragraphs/numbering rows), and, in the course of building that proof
honestly, to find and fix one genuine bug (the furniture-boundary over-absorption of
Categories ch.4's body table — see the footnotes row above) rather than paper over
it in the pin.

Two things that would still need a NEW row (or a NEW annotation) if this checklist
were being re-scoped from scratch, but are out of Phase 5's stated task list so left
as forward pointers rather than added rows: (1) De Interpretatione has not itself
been run through the converter — Phase 5 §1's bare-numeral grammar is proven against
Categories only, and De Int is named in the spec purely by analogy (same single-book
shape); (2) the bare-numeral acceptance gate's "exactly one-chapter forward gap"
tolerance (divisions.ts) is unit-tested but has no real-slice case exercising it (the
Categories slice's own chapter sequence has no gaps) — proven correct by
construction and by the synthetic test, not by a real editorially-skipped chapter
number.
