v0.2.0 rather than v0.1.0 because 0.1.0 already exists internally as the unreleased Tauri scaffold — reusing it would misrepresent a first public build as a re-release. A 0.x minor bump (not 1.0) signals the app is functional and feature-complete for this use case but still pre-1.0: unsigned, no auto-update yet, and one known gap (annotation capture in compare view). Semantic-versioning convention reserves 1.0 for the first release the author considers stable and complete, which this deliberately is not yet.
